/*
Namn
Datum

Uppgift 7 - Funktioner

Fyll i funktionerna så att de gör det som kommentaren säger.

Kursmoment som ingår i uppgiften:
Funktioner
*/

using System;


class Program
{
    static void Main()
    {
        // Använd funktionerna här
    }

    // Funktion som tar in två heltal och returnerar summan av dem
    static int Addera(int tal1, int tal2)
    {
        // Fyll i kod här
    }

    // Funktion som tar in två heltal och returnerar det största av dem
    static int Största(int tal1, int tal2)
    {
        // Fyll i kod här
    }



    // Funktion som tar in en sträng och returnerar antalet bokstäver i strängen
    static int AntalBokstäver(string text)
    {
        // Fyll i kod här
    }

    //Funktion som tar in en sträng (name) och skriver ut att personen är bäst
    static void Bäst(string name)
    {
        // Fyll i kod här
    }

    //Funktion som tar in ett heltal och returnerar true om det är ett primtal, annars false
    static bool Primtal(int tal)
    {
        // Fyll i kod här
    }
}



